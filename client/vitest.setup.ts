import '@testing-library/jest-dom/vitest';

// This file runs before each test suite
// It extends Vitest's expect with custom matchers from @testing-library/jest-dom
// Examples: toBeInTheDocument(), toBeDisabled(), toHaveClass(), etc.
